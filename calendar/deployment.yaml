apiVersion: v1
kind: Service
metadata:
  name: calendar
  namespace: calendar
spec:
  type: ClusterIP
  selector:
    app: calendar
  ports:
    - port: 80
      targetPort: 8080
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: calendar
  namespace: calendar
  labels:
    app: calendar
spec:
  replicas: 1
  selector:
    matchLabels:
      app: calendar
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: calendar
    spec:
      restartPolicy: Always
      containers:
      - name: calendar
        image: ghcr.io/jelmer/xandikos:latest
        ports:
        - containerPort: 80
          name: http
        volumeMounts:
        - name: calendar-volume
          mountPath: /data
      volumes:
      - name: calendar-volume
        persistentVolumeClaim:
          claimName: calendar-pvc
